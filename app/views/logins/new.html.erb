<%= form_for @user, url: login_url, method: :post do |f| %>
  <% fi = FormItem.new(f) %>

  <%= fi.text_field(:email, 'email') %>
  <%= fi.password(:password, 'password') %>
  <%= fi.submit('login') %>
<% end %>