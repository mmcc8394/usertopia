<div class="row">
  <div class="col-md-6">
    <%= form_for @user do |f| %>
      <% fi = FormItem.new(f) %>

      <%= fi.text_field(:email, 'email') %>
      <%= fi.submit('Make it happen!') %>
    <% end %>
  </div>
</div>