<%= form_item.text_field(:first_name, 'first name') %>
<%= form_item.text_field(:last_name, 'last name') %>
<%= form_item.text_field(:email, 'email') %>
<% if current_user.admin? %>
  <%= form_item.checkbox_collection(:roles, User.roles.values, :to_s, :to_s) %>
<% end %>