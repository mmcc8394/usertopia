<h1>List of Users</h1>

<p>
  <%= link_to 'Add New User', new_admin_user_path %>
</p>

<table class="table">
  <% @users.each do |user| %>
    <tr>
      <td><%= user.full_name %></td>
      <td>[<%= user.print_roles %>]</td>
      <td>
        <%= action_link(admin_user_path(user), 'eye', 'view user') %>
        <%= action_link(edit_admin_user_path(user), 'pencil', 'edit user') %>
        <%= action_link(edit_password_admin_user_path(user), 'shield-lock', 'change password') %>
        <%= action_link_delete(admin_user_path(user)) %>
      </td>
    </tr>
  <% end %>
</table>